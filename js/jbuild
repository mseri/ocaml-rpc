(library
   ((name rpc_client_js)
    (public_name rpclib.js)
    (optional)
    (libraries (rpclib.core js_of_ocaml))
    (modules (rpc_client_js rpc_client_js_helper))
    (preprocess (pps (js_of_ocaml.ppx)))
   ))
