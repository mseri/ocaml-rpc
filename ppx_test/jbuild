(executables
 ((names (test tytest))
  (public_names (test tytest))
  (libraries (ppx_deriving ppx_deriving_rpc rpclib rpclib_core json lwt lwt.unix result rresult oUnit))
  (preprocess (pps (ppx_deriving_rpc)))
  (package ppx_deriving_rpc)
 ))

(alias
 ((name   runtest)
  (deps   (test.exe))
  (action (run ${<}))))

(alias
 ((name   runtest)
  (deps   (tytest.exe))
  (action (run ${<}))))
